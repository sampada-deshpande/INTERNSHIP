DATE: 17/12/2025


What are Data Structures?

Data structures are ways to store, organize, and manage data so that it can be used efficiently.

In Python, we mainly use these built-in data structures:

✅ 1. List

Ordered, mutable (changeable), allows duplicates

📍 Used when you need a collection that can change.

marks = [45, 67, 89, 67]
print(marks)

marks.append(90)      # add
marks[0] = 50         # update
marks.remove(67)     # remove
print(marks)


🔹 Operations:

append(), insert(), extend()

remove(), pop()

sort(), reverse()

Indexing & slicing

✅ 2. Tuple

Ordered, immutable (cannot change), allows duplicates

📍 Used for fixed data.

point = (10, 20, 30)
print(point)

print(point[1])   # access


🔹 Operations:

Indexing, slicing

count(), index()

Faster & safer than list for fixed data

❌ Cannot add, remove, or update items.

✅ 3. Set

Unordered, mutable, no duplicates

📍 Used when you want unique values.

nums = {1, 2, 3, 3, 4}
print(nums)   # duplicates removed

nums.add(5)
nums.remove(2)
print(nums)


🔹 Operations:

add(), remove(), discard()

Set math: union(), intersection(), difference()

a = {1, 2, 3}
b = {3, 4, 5}
print(a.union(b))
print(a.intersection(b))

✅ 4. Dictionary (dict)

Key–value pairs, unordered (ordered from Python 3.7+), mutable, no duplicate keys

📍 Used to map data like name → marks.

student = {
    "name": "Amit",
    "roll": 101,
    "marks": 85
}

print(student["name"])

student["marks"] = 90   # update
student["grade"] = "A" # add
del student["roll"]    # delete

print(student)


🔹 Operations:

Access by key

keys(), values(), items()

get(), pop(), update()